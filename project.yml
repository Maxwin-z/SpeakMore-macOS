name: SpeakMoreLite
options:
  bundleIdPrefix: cn.byutech
  deploymentTarget:
    macOS: "14.0"
  generateEmptyDirectories: true

targets:
  SpeakMoreLite:
    type: application
    platform: macOS
    sources:
      - Sources/SpeakMoreLite
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: cn.byutech.SpeakMoreLite
        INFOPLIST_FILE: Sources/SpeakMoreLite/Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: SpeakMoreLite.entitlements
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: BS4GBZN537
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
